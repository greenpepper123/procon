#include <iostream>
#include <cmath>
#include <vector>
#include <string>
#include <algorithm>

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);
    int n;
    std::cin >> n;
    int p[501], q[501];
    for (int i=1; i<=n; i++) {
        std::cin >> p[i];
    }
    for (int i=0; i<=n; i++) {
        std::cin >> q[i];
    }
    

    return 0;
}
